// 7种原始类型
// number string boolean bigint undefined null symbol

let isDone: boolean = false;

isDone = true;

let myName: string = `Aybuai`;

let age: number = 18;

let message: string = `hello, ${myName}`;

let u: undefined = undefined;

let n: null = null;
